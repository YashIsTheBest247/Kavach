# Copy to backend/.env and fill in to enable the Gemini AI augmentation layer.
# Without this, the platform runs fine on the deterministic rule engine alone.
GEMINI_API_KEY=your_google_ai_studio_key_here

# Optional — defaults to gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash
