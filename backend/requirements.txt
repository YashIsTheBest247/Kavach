# Python 3.11+ (tested on 3.14). Versions below ship prebuilt wheels for 3.14.
fastapi>=0.138.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
pydantic>=2.13.0
Pillow>=11.0.0
numpy>=2.0.0
google-genai>=1.0.0
python-dotenv>=1.0.0
APScheduler>=3.10.0
fpdf2>=2.8.0            # court-admissible PDF reports (/api/report/scam/pdf)
edge-tts>=7.0.0        # neural reel narration (EN/HI, male/female)
imageio-ffmpeg>=0.5.0  # bundled ffmpeg: reel render + Telegram voice-note decode
python-telegram-bot>=22.0  # Telegram Fraud Shield — webhook on the deployed API + local polling

# --- Telegram bot ---
# Deployed: set TELEGRAM_BOT_TOKEN (from @BotFather); the API auto-registers a
#   webhook at /api/telegram/webhook using RENDER_EXTERNAL_URL. No extra process.
# Local:    run `python -m app.telegram_bot` for long polling instead.

# --- Optional: local video-render worker + YouTube auto-publish ---
# Not needed on the free cloud tier; install locally to produce & publish the MP4:
#   pip install kokoro soundfile moviepy                              # render (needs ffmpeg)
#   pip install google-api-python-client google-auth-oauthlib google-auth-httplib2   # YouTube upload
#   + install ffmpeg, set PEXELS_API_KEY (visuals) & GEMINI_API_KEY (script),
#   run `python -m app.youtube_upload` once for OAuth, then ENABLE_YOUTUBE_UPLOAD=1
