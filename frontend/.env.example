# Frontend → backend API base URL (must end with /api).
# Local dev leaves this unset (Vite proxies /api to localhost:8000).
# Production (Vercel): set to your deployed backend, e.g.
# VITE_API_URL=https://kavach-api.onrender.com/api
REMOVED
